part of '../tui_command.dart';

class _TuiTheme {
  static const background = Color(0.035, 0.042, 0.052);
  static const panel = Color(0.065, 0.078, 0.096);
  static const panelAlt = Color(0.082, 0.096, 0.118);
  static const selection = Color(0.08, 0.30, 0.36);
  static const border = Color(0.20, 0.28, 0.34);
  static const borderActive = Color(0.38, 0.78, 0.88);
  static const text = Color(0.82, 0.88, 0.92);
  static const muted = Color(0.52, 0.60, 0.66);
  static const accent = Color(0.48, 0.88, 0.94);
  static const project = Color(0.52, 0.94, 0.66);
  static const global = Color(0.92, 0.76, 0.42);
  static const warning = Color(1, 0.62, 0.36);

  const _TuiTheme._();
}
