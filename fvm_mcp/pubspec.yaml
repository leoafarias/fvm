name: fvm_mcp
description: MCP server exposing FVM (Flutter Version Management) as tools
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  dart_mcp: ^0.3.3
  stream_channel: ^2.1.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
