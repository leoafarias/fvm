name: fvm_mcp
description: MCP server exposing FVM (Flutter Version Management) as tools
version: 0.0.1-alpha.1
repository: https://github.com/leoafarias/fvm/tree/main/fvm_mcp

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  dart_mcp: ^0.3.3
  process: ^5.0.5
  pub_semver: ^2.2.0
  stream_channel: ^2.1.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
